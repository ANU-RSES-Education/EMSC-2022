# This file is what mybinder.org reads.
#
# It points at a pre-built image produced by
# .github/workflows/build-binder-image.yml from .binder/Dockerfile.source.
#
# Rebuilding on Binder's overloaded shared cluster was unreliable; pulling a
# pre-built image is fast and deterministic. To rebuild the image, push a
# change touching anything under .binder/ — the GH Action re-runs automatically.

FROM ghcr.io/anu-rses-education/emsc-2022:latest
